package com.iam.security;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeIamSecurityResourceIT extends IamSecurityResourceTest {

    // Execute the same tests but in native mode.
}